<h3>@Localizer["item_list"]</h3>

<EditForm Model="this">
    <InputText id="search" @bind-Value="Text" @oninput="searching"/>
    <button @onclick="searching">@Localizer["search"]</button>
</EditForm>

@*Je pense que quelque chose de ce genre corrigerais le listing par défaut lors du chargement de la page
@DotNet.invokeMethodAsync("searching");*@

@if (!haveAnyItem)
{
    <h3 style="color:red">@Localizer["error_message"]</h3>
}
else
{
    @foreach (var item in itemsTrouve)
    {
        <Inventory Item="item" NoDrop="true" />
    }
}
